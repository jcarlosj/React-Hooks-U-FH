/** Define funcion pura como reducer (controlar acciones) */
export const todoReducer = ( state = [], action ) => {

    switch ( action.type ) {
        // case 'ADD':
            
        //     break;
    
        default:
            return state;
    }

}